[options]
; This is the password that allows database operations:
; admin_passwd = admin
db_host = False # (WSL) contain the ip address of local
db_port = False # (WSL) contain database port, which is 5432 if not changed
db_user = False
db_password = False
;addons_path = /usr/lib/python3/dist-packages/odoo/addons
default_productivity_apps = True
db_name = odoo 
